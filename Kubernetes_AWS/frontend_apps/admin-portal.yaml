# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: admin-portal
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: admin-portal
#   template:
#     metadata:
#       labels:
#         app: admin-portal
#     spec:
#       nodeSelector:
#         admin-portal: NodeGroup
#       containers:
#       - name: admin-portal
#         image: 767397723308.dkr.ecr.us-east-1.amazonaws.com/aline_financial_repo:admin-portal-fafo
#         imagePullPolicy: IfNotPresent
#         ports:
#         - containerPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: admin-portal-service
spec:
  # type: NodePort
  type: LoadBalancer
  selector:
    app: admin-portal  # Match the labels used by your user microservice deployment
  ports:
    - protocol: TCP
      port: 3000   # Port used by user microservice
      targetPort: 80  # Port on the user microservice pod to forward traffic to